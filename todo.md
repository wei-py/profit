<!--1. 预设选中的国家平台，要有持久缓存
2. 选项页面分组选中也是持久缓存，还有分组这里的布局宽度小一点
3. 预设和选项页面都和模板页面左边的规则集一样宽就行
4. 新建页面右侧的也是宽度小一点
5. 名称就是字段键，后续不需要显示字段键这个，记得 excel-rule-engine-example.xlsx 跟进一下-->

<!--6. 预设选中的时候 样式变一下， 选项页面也是, 模板页面也是
7. 模板页面也要选中持久化-->
<!-- 8. 预设、选项、模板页面的按钮左侧的菜单按钮宽度拉满呀-->
<!--9. 预设页面的类型是下拉选项的，如果类型是 select 的话，默认值也是下拉，类型的选择也是中文-->
<!--10. 表格的高度不是无限的，也就是预设、选项、模板页面的高度 h-screen, 表格滚动以及列表滚动就行
11. 默认高度到底，并且固定表头
12. 预设、选项、模板、页面    是表格内部滚动，是列表内部滚动，而且表格默认高度是到底，你能理解吗-->

<!--1. 国家平台页面 第一层级也是弹窗呀，不过是点击任意地方展开，多一个操作列（编辑，删除），点击编辑就是弹窗
2. 计算模板，不要叫做费率表，并且可以添加多个表以及删除。可以改表名（在弹窗中）
3. 编辑规则，条件（同行=AND，多行同输出字段键=OR）是多条的，且删除的

4. 计算模板, 查表名称应该是下拉的
5. 计算字段，输入的话多一个字段默认值（下拉类型和布尔则是下拉选择，数字和文本则是输入）-->

<!--6. 模板添加条件只能 AND 吗？还有如果有复杂的条件运算，你有什么好的方案推荐吗，该如何处理，你规划一下-->
<!--7. 字段，输出的话不用多默认值这个字段-->

<!--8. 模板 编辑规则 条件组（可 and 可 or）要可以添加删除，内部添加也是可以添加删除（and 和 or）

不要分什么条件组了，因为可能也有内部条件组这种情况，你看看有什么好的方法-->

<!--1. 最多 4 个条件？？？我点添加就有这个了，当前没有添加啊，为什么设置4个条件呢，代码不是可以递归的吗
2. 配置页面，货币符号、汇率，就这一块的表头不要有删除的按钮呀-->

<!--"+ 条件"  改为 2 个 "+ AND 条件" 和 "+ OR 条件", 并且加完之后可以单击自动切换成另外的条件-->

<!--单击条件，其他的条件也都变了，这不对吧，还有子组是可以无限层的呀-->

<!--1. 不对呀，不只是外层的AND/OR, 可以单击切换啊
2. 还有同层的条件为什么只能 AND 或者 只能 OR-->

<!--3. 查表数据，新建表没有反应，查表添加列也没有反应-->

<!--2.0.5 每个弹窗都加上教学引导一下，右上角显示个问号，点击就开始引导，默认刚刚开始都不引导，用户点击才开始引导当前的弹窗介绍操作-->

<!--国家平台这几个字变成配置，excel配置也跟进一下，还有如果没有绑定，点击保存的时候要选择保存地址自动生成 excel 文件，中间要我命名文件名。如果提前绑定，保存的话就更新数据-->

<!--配置文件 和 商品列表 是 2 个 excel文件-->

<!--商品ID呢？商品类目要下拉选择啊，是否包邮也是，SKU要可以修改的呀，还有点击生成SKU也没有反应，商品记录的操作要固定列-->

<!--也不用什么菜单栏了， 直接使用 tab, 主题变化按钮就在右上角吧，帮助也是-->

<!--1. 单位为 % 号， 要 * 0.01-->
<!--2. 图片要可以上传，到 excel 文件中去(也可以删除，点击图片可以预览大图支持缩放)，并且要能嵌入到单元格中去（wps）-->
<!--3. 计算过程-->

<!--4. 配合生成 sku 按钮
自定义前缀添加一下，可以输入
款号就是从 001 开始加起来
sku = 自定义前缀 + 款号 + 变体属性
5. 配置、选项组、模板的上下移动按钮没有反应

6. 还要加一个功能，通过利润率、净利润、售价 三者互相运算求值(修改其中 1 位自动算其他 2 位，这里要注意代码容易混乱逻辑运算)，你觉得在 sku 列表中每一项加一个弹窗去运行好，还是有什么更好的其他方案呢
7. 编辑列弹窗变成使用输入排序值，来改变吧，保存后， 还有弹窗里的项使用grid,3 * 3
  布局，还有sku的编辑列也要包括计算字段呀，准确来说是全部字段-->

<!--商品记录也要可以上下移动，里面的图片也要可以预览，还要加一个按钮，可以获取到相同的商品ID到新建商品中去-->
<!--你会保存图片使用的是 base64 吧，不要这样的呀-->
<!--图片要嵌入单元格呀，现在并没有呢，你看看https://www.wps.cn/learning/question/detail/id/2191.html-->
<!--图片要嵌入单元格呀，现在并没有呢 你看看https://bbs.wps.cn/topic/3862, 好像是使用 =DISPIMG("ID_DAB6B79A83264B9F9915B67B629DA250",1)-->

<!--配置页面最外层要可以上下移动,里面的字段、选项组、模板 这些列表也要可以上下移动，包括商品列表里的sku的那个列表也可以上下移动-->

<!--dist文件夹能上传到cloudflare的 page 去，name是profit-->
 <!--pnpm dlx wrangler pages deploy dist --project-name profit
 pnpm build && pnpm dlx wrangler pages deploy dist --project-name profit-->

<!--所有的计算字段， 比如销售佣金费率 旁边要有个问号，点击后弹窗说明一下计算的过程要详细一点，例如销售佣金费率，查了类目佣金查询表，刊登类型寿是什么，类目是什么，所以费率为什么，如果是其他的百分比、公式、加总等等，你也适配一下-->
<!--5. 要有这样的操作可以，点击 售价统一（成本价，重量）,内容就变一样
6. 单位为 % 号， 要 * 0.01-->

<!--算了，还是不行，excel文件需要多一个字段图片路径, 需要有一个能保存到软件内容的图片路径，统一到一起，图片字段还是嵌入表格 ，
  图片路径字段就是保存到软件内部的图片路径，打开列表就是去读取这个图片路径的图片，现在到页面上去，我看了一下之前是这样解决的-->

<!--能不能也兼容 web 端呢-->

<!--3. 商品列表 持久化 (国家平台、模板)
4. 商品列表 持久化 (国家平台、模板)，我选完之后，点击去配置页面，回来就丢失了
5. 商品列表，商品级字段， 刊登类型、商品类目、是否包邮 这些在配置中是属于下拉类型的，就变成下拉选项呀，不要输入 -->

 <!--build-exe-->
<!--Node.js 20 is deprecated. The following actions target Node.js 20 but are being forced to run on Node.js 24: actions/checkout@v4, actions/setup-node@v4, actions/upload-artifact@v4, pnpm/action-setup@v4. For more information see: https://github.blog/changelog/2025-09-19-deprecation-of-node-20-on-github-actions-runners/-->

拖拽三条杠无法移动位置，注意要能同步到 excel 文件中去，还有编辑列配合能够改为表格的列顺序
@shopify/draggable
